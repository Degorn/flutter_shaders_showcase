name: shaders
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
  shaders:
    - assets/shaders/flask_chart.frag
    - assets/shaders/glow_screens.frag
    - assets/shaders/gradient.frag
    - assets/shaders/mandelbrot_set.frag
    - assets/shaders/rotating_card.frag
    - assets/shaders/shader.frag